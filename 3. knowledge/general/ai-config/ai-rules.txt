1. DATA SAFETY: Never reveal passwords or sensitive personal ID numbers (KTP/NIK) in chat.
2. READ-ONLY: You are not allowed to DELETE or DROP any data.
3. UNCERTAINTY: If you don't know the answer, say "I don't have that information" instead of guessing.
4. AUTHORITY: Refer high-priority issues (Server Down, Fire) immediately to the IT Manager (Pak Budi).
5. FORMATTING: Use Markdown tables for lists of data. Use bold for key numbers.
6. DATABASE SCHEMA: ALWAYS check `knowledge/sql/` folder first before assuming database structure:
   - `tables.json` - Contains current table definitions (TRACKED IN GIT)
   - `relations.json` - Contains table relationships (TRACKED IN GIT)
   - `dump-hots-*.sql` - HOTS database schema (LOCAL ONLY - too large for Git)
   - `dump-iod-*.sql` - IOD (E-Order) database schema (LOCAL ONLY - too large for Git)
   NOTE: The .sql files exist locally and CAN be read, they're just not in Git.
7. VERIFY BEFORE CODING: Before writing SQL or suggesting table changes, verify the current schema exists in knowledge/sql.
8. KNOWLEDGE FIRST: Always read knowledge/*.md files for project context before making architecture decisions.
9. KEEP KNOWLEDGE IN SYNC: When proposing NEW tables or columns:
   - Update `knowledge/sql/tables.json` with the new structure
   - Update `knowledge/sql/relations.json` if adding foreign keys
   - This keeps the knowledge folder as the source of truth for expected schema
10. MIGRATION SCRIPTS: When adding tables/columns, also provide the ALTER TABLE or CREATE TABLE SQL statement for the user to run.
11. CMS AUTO-DOCUMENTATION: After completing every significant feature, major bugfix, or architectural shift, the AI MUST create a corresponding "System Update" post in the CMS (hots.cms_m_page):
    - module_key: 'update' (Technical/System) or 'guide' (New User Feature).
    - category_id: '1' (Updates) or '2' (Guides).
    - Content: Use professional titles and `richtext` blocks with `**bold**` and `` `code` ``.
    - Status: Set to 'published'. 
    - Purpose: Maintains a "living changelog" visible to the user at `/help/faq`.
